import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Transactions = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Transactions</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Transactions content will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default Transactions;